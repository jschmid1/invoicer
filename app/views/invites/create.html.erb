<%= form_for @invite , :url => invites_path do |f| %>
    <%= f.hidden_field :flat_id, :value => @invite.flat_id %>
    <%= f.label :email %>
    <%= f.email_field :email %>
    <%= f.submit 'Send' %>
<% end %>