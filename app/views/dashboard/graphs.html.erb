Chart for Balances
<%= bar_chart User.where(flat_id: current_user.flat_id).group(:name).sum(:balance) %>

Chart for the last 30 days
<%= area_chart Bill.where(user_id: User.where(flat_id: current_user.flat_id)).where(created_at: Time.now-30.days..Time.now).group_by_day(:created_at).maximum(:value) %>

<%= bar_chart Market.group(:name).sum(:count) %>
Market is still wrong because has no connection to Flats at all


